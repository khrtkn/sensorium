{
  "name": "livekit-token",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prebuild": "cp -f ../../tsconfig.json ./tsconfig.base.json || true",
    "start": "node index.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
